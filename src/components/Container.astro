---
interface Props {
	class?: string;
	as?: string;
}
const { class: className = "", as: Tag = "div" } = Astro.props;
---

<Tag class:list={["mx-auto w-full max-w-[1200px] px-6 sm:px-8 lg:px-10", className]}>
	<slot />
</Tag>
